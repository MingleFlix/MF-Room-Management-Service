meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{base_url}}/rooms
  body: json
  auth: none
}

body:json {
  {
    "name": "Conference Room"
  }
  
}

tests {
  test("Status code is 201", function () {
      expect(res.getStatus()).to.equal(201);
  });
  
  test("Response time is less than 500ms", function () {
      expect(res.getResponseTime()).to.be.below(500);
  });
  
  test("Response contains room details", function () {
      let data = res.getBody();
      expect(data).to.have.property("name", "Conference Room");
      expect(data).to.have.property("id");
    
      bru.setEnvVar("id",data.id)
  });
  
}
