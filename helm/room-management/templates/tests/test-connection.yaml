apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "room-management.fullname" . }}-test-connection"
  labels:
    {{- include "room-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "room-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
